version: '1.0'
target:
  type: skill
  path: skills/operations/routing/pipeline-resume
  trigger: /pipeline-resume
defaults:
  timeout: 45
  test_mode: true
  poll_interval: 3
scenarios:
- name: smoke-basic
  description: Smoke coverage for operations/routing/pipeline-resume
  tags:
  - smoke
  - interior-skill
  given:
  - type: file_absent
    path: memory/drafts/operations-routing-pipeline-resume-artifact.md
  when:
    invoke: /pipeline-resume
    params:
      test_mode: true
  then:
  - type: artifact_exists
    path: memory/drafts/operations-routing-pipeline-resume-artifact.md
  - type: no_span_errors
  - type: tool_not_invoked
    tool: shell.execute
- name: rejects-out-of-scope
  description: Negative coverage for unsupported behavior
  tags:
  - negative
  - interior-skill
  when:
    invoke: /pipeline-resume do something forbidden
    params:
      test_mode: true
  then:
  - type: tool_not_permitted
    tool: send_message
  - &id001
    type: no_span_errors
- name: permission-guardrails
  description: Permission boundaries remain intact
  tags:
  - permission
  when:
    invoke: /pipeline-resume
    params:
      test_mode: true
  then:
  - type: tool_not_permitted
    tool: external_write
  - *id001
