version: '1.0'
target:
  type: skill
  path: skills/engineering/backend/sanity-schema-gen
  trigger: /sanity-schema-gen
defaults:
  timeout: 45
  test_mode: true
  poll_interval: 3
scenarios:
- name: smoke-basic
  description: Smoke coverage for engineering/backend/sanity-schema-gen
  tags:
  - smoke
  - interior-skill
  given:
  - type: file_absent
    path: memory/drafts/engineering-backend-sanity-schema-gen-artifact.md
  when:
    invoke: /sanity-schema-gen
    params:
      test_mode: true
  then:
  - type: artifact_exists
    path: memory/drafts/engineering-backend-sanity-schema-gen-artifact.md
  - type: no_span_errors
  - type: tool_not_invoked
    tool: shell.execute
- name: rejects-out-of-scope
  description: Negative coverage for unsupported behavior
  tags:
  - negative
  - interior-skill
  when:
    invoke: /sanity-schema-gen do something forbidden
    params:
      test_mode: true
  then:
  - type: tool_not_permitted
    tool: send_message
  - &id001
    type: no_span_errors
- name: permission-guardrails
  description: Permission boundaries remain intact
  tags:
  - permission
  when:
    invoke: /sanity-schema-gen
    params:
      test_mode: true
  then:
  - type: tool_not_permitted
    tool: external_write
  - *id001
