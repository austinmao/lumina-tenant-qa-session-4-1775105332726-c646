version: '1.0'
target:
  type: skill
  path: skills/platform/compiler/scaffold-interview-policy
  trigger: /scaffold-interview-policy
defaults:
  timeout: 60
  test_mode: true
  poll_interval: 5
scenarios:
- name: smoke-basic
  description: Smoke coverage for platform/compiler/scaffold-interview-policy
  tags:
  - smoke
  - boundary-skill
  given:
  - type: file_absent
    path: memory/drafts/platform-compiler-scaffold-interview-policy-artifact.md
  when:
    invoke: /scaffold-interview-policy
    params:
      test_mode: true
  then:
  - type: artifact_exists
    path: memory/drafts/platform-compiler-scaffold-interview-policy-artifact.md
  - type: no_span_errors
  - type: tool_not_invoked
    tool: shell.execute
- name: rejects-out-of-scope
  description: Negative coverage for unsupported behavior
  tags:
  - negative
  - boundary-skill
  when:
    invoke: /scaffold-interview-policy do something forbidden
    params:
      test_mode: true
  then:
  - type: tool_not_permitted
    tool: send_message
  - &id001
    type: no_span_errors
- name: permission-guardrails
  description: Permission boundaries remain intact
  tags:
  - permission
  when:
    invoke: /scaffold-interview-policy
    params:
      test_mode: true
  then:
  - type: tool_not_permitted
    tool: external_write
  - *id001
