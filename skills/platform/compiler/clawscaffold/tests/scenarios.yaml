version: '1.0'
target:
  type: skill
  path: skills/platform/compiler/clawscaffold
  trigger: /clawscaffold
defaults:
  timeout: 45
  test_mode: true
  poll_interval: 3
scenarios:
- name: smoke-basic
  description: Smoke coverage for platform/compiler/clawscaffold
  tags:
  - smoke
  - interior-skill
  given:
  - type: file_absent
    path: memory/drafts/platform-compiler-clawscaffold-artifact.md
  when:
    invoke: /clawscaffold
    params:
      test_mode: true
  then:
  - type: artifact_exists
    path: memory/drafts/platform-compiler-clawscaffold-artifact.md
  - type: no_span_errors
  - type: model_used
    expected: gemini-2.5-flash-lite
  - type: tool_not_invoked
    tool: shell.execute
- name: rejects-out-of-scope
  description: Negative coverage for unsupported behavior
  tags:
  - negative
  - interior-skill
  when:
    invoke: /clawscaffold do something forbidden
    params:
      test_mode: true
  then:
  - type: tool_not_permitted
    tool: send_message
  - &id001
    type: no_span_errors
- name: permission-guardrails
  description: Permission boundaries remain intact
  tags:
  - permission
  when:
    invoke: /clawscaffold
    params:
      test_mode: true
  then:
  - type: tool_not_permitted
    tool: external_write
  - *id001
