version: '1.0'
target:
  type: skill
  path: skills/platform/governance/governance-audit
  trigger: /governance-audit
defaults:
  timeout: 45
  test_mode: true
  poll_interval: 3
scenarios:
- name: smoke-basic
  description: Smoke coverage for platform/governance/governance-audit
  tags:
  - smoke
  - interior-skill
  given:
  - type: file_absent
    path: memory/drafts/platform-governance-governance-audit-artifact.md
  when:
    invoke: /governance-audit
    params:
      test_mode: true
  then:
  - type: artifact_exists
    path: memory/drafts/platform-governance-governance-audit-artifact.md
  - type: no_span_errors
  - type: tool_not_invoked
    tool: shell.execute
- name: rejects-out-of-scope
  description: Negative coverage for unsupported behavior
  tags:
  - negative
  - interior-skill
  when:
    invoke: /governance-audit do something forbidden
    params:
      test_mode: true
  then:
  - type: tool_not_permitted
    tool: send_message
  - &id001
    type: no_span_errors
- name: permission-guardrails
  description: Permission boundaries remain intact
  tags:
  - permission
  when:
    invoke: /governance-audit
    params:
      test_mode: true
  then:
  - type: tool_not_permitted
    tool: external_write
  - *id001
