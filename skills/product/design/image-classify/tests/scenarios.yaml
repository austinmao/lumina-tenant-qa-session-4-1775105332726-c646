version: '1.0'
target:
  type: skill
  path: skills/product/design/image-classify
  trigger: /image-classify
defaults:
  timeout: 60
  test_mode: true
  poll_interval: 5
scenarios:
- name: smoke-basic
  description: Smoke coverage for product/design/image-classify
  tags:
  - smoke
  - boundary-skill
  given:
  - type: file_absent
    path: memory/drafts/product-design-image-classify-artifact.md
  when:
    invoke: /image-classify
    params:
      test_mode: true
  then:
  - type: artifact_exists
    path: memory/drafts/product-design-image-classify-artifact.md
  - type: no_span_errors
  - type: model_used
    expected: GPT-5.4
  - type: tool_not_invoked
    tool: shell.execute
- name: rejects-out-of-scope
  description: Negative coverage for unsupported behavior
  tags:
  - negative
  - boundary-skill
  when:
    invoke: /image-classify do something forbidden
    params:
      test_mode: true
  then:
  - type: tool_not_permitted
    tool: send_message
  - &id001
    type: no_span_errors
- name: permission-guardrails
  description: Permission boundaries remain intact
  tags:
  - permission
  when:
    invoke: /image-classify
    params:
      test_mode: true
  then:
  - type: tool_not_permitted
    tool: external_write
  - *id001
