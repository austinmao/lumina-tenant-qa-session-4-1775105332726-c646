version: '1.0'
target:
  type: skill
  path: skills/programs/onboarding/participant-prep
  trigger: /participant-prep
defaults:
  timeout: 45
  test_mode: true
  poll_interval: 3
scenarios:
- name: smoke-basic
  description: Smoke coverage for programs/onboarding/participant-prep
  tags:
  - smoke
  - interior-skill
  given:
  - type: file_absent
    path: memory/drafts/programs-onboarding-participant-prep-artifact.md
  when:
    invoke: /participant-prep
    params:
      test_mode: true
  then:
  - type: artifact_exists
    path: memory/drafts/programs-onboarding-participant-prep-artifact.md
  - type: no_span_errors
  - type: tool_not_invoked
    tool: shell.execute
- name: rejects-out-of-scope
  description: Negative coverage for unsupported behavior
  tags:
  - negative
  - interior-skill
  when:
    invoke: /participant-prep do something forbidden
    params:
      test_mode: true
  then:
  - type: tool_not_permitted
    tool: send_message
  - &id001
    type: no_span_errors
- name: permission-guardrails
  description: Permission boundaries remain intact
  tags:
  - permission
  when:
    invoke: /participant-prep
    params:
      test_mode: true
  then:
  - type: tool_not_permitted
    tool: external_write
  - *id001
