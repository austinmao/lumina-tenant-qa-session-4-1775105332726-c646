{
  "id": "clawwrap",
  "name": "ClawWrap Gateway Bridge",
  "description": "Gateway bridge for the ClawWrap outbound policy and conformance engine.",
  "version": "0.1.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "repoRoot": { "type": "string", "minLength": 1 },
      "pythonBin": { "type": "string", "minLength": 1 },
      "timeoutMs": { "type": "integer", "minimum": 1000, "default": 60000 }
    }
  }
}
