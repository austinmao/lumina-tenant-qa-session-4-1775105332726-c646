{
  "id": "clawinterview",
  "name": "ClawInterview Gateway Bridge",
  "description": "Gateway bridge for scaffold interview compilation, validation, and execution.",
  "version": "0.1.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "repoRoot": { "type": "string", "minLength": 1 },
      "pythonBin": { "type": "string", "minLength": 1 },
      "timeoutMs": { "type": "integer", "minimum": 1000, "default": 60000 }
    }
  }
}
